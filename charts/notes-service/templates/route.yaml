{{- if .Values.route.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "notes-service.fullname" . }}
spec:
  to:
    kind: Service
    name: {{ include "notes-service.fullname" . }}
  port:
    targetPort: 3000
  tls:
    termination: edge
{{- end }}
